"use client";

interface HomeFiltersProps {
  onChange: (filters: any) => void;
}

export default function HomeFilters({ onChange }: HomeFiltersProps) {
  return (
    <div>
      Home Filters Placeholder
    </div>
  );
}

